# 사내 AI-DLC Standalone Agent 설계

문서 상태: 기본 설계와 사용자 합의. 상세 설계 D1의 구현 기준은 [DETAILED_DESIGN.md](DETAILED_DESIGN.md), [WORKFLOW_SPEC.md](WORKFLOW_SPEC.md), [RUNTIME_SPEC.md](RUNTIME_SPEC.md)에 확정했다. 실제 내부 API·OS·배포 연동과 환경 검증은 아직 수행하지 않았다.

첫 구현 묶음은 [README.md](README.md)를 따른다. 설정·모델 선택·로컬 평가 CLI를 구현했고 실제 API·승인/상태 복구·빌드·배포·웹은 후속 범위다.

다중 모델 확장 D2: [MULTI_MODEL_SPEC.md](MULTI_MODEL_SPEC.md)에 따라 여러 provider/model 등록과 목적별 선택을 구성한다. 사내 환경의 계약 확인은 현재 진행하지 않고 이후 연결 단계로 미룬다.

## 1. 목표와 합의된 경계

- 여러 도메인의 repository에 같은 standalone Agent를 적용한다. 요구사항과 실행 범위는 repository별로 독립적이다.
- 분석, 설계, 구현, 검증, 리뷰, 릴리스, 배포, 운영 피드백을 연결한다. 단계적으로 개발하되 전체 과정의 운영 검증이 끝나기 전에는 운영 도입하지 않는다.
- GitHub Enterprise Server와 호출 가능한 사내 모델 API를 사용한다. GHES 버전과 모델 API 규격은 확인이 필요하다.
- Agent는 RHEL 7·8·9 모두를 지원 대상으로 한다. 각 환경에서의 설치·실행 검증은 아직 수행하지 않았다. 컨테이너는 사용하지 않는다.
- 현재 운영 도입 범위는 Java 8 기반 프로젝트이며 JUnit unit test가 중심이다. 사내 Maven 저장소를 활용한다. C#은 후속 지원으로 두며 현재 Java 대상 SDLC 전 과정 구현의 선행 조건으로 삼지 않는다.
- Gemma4·gpt-oss·DeepSeek·Gauss 등 여러 사내 모델을 등록하고 repository·목적별로 선택할 수 있게 한다. 모델명과 API protocol을 분리하고 공통 모델 인터페이스를 사용한다. 실제 인증·요청·응답·도구 기능은 미확인이며 현재 확인을 요구하지 않는다. 미구성 값을 추측해 호출하지 않고 후속 연결 단계에서 채운다.
- 사내 GHES는 SSO 기반 로그인이다. GHES에서 Agent 내부 webhook 주소로의 접근은 가능할 것으로 예상되나 실제 연결은 미검증이다.
- Jira 연동은 현재 구현 범위에서 제외한다. 요구사항 입력은 GitHub Issue를 기준으로 한다.
- 운영 환경에서는 사외 데이터 전송 및 외부 접속 시도를 금지한다. 소스, 프롬프트, 응답, 로그, 모델, 패키지, SDK, 도구 의존성 모두 승인된 내부 경계에서 취급한다.
- 사용자가 명시적으로 선택한 테스트 환경에서는 설정 파일로 GPT API와 사외 Maven 저장소를 사용할 수 있다. 이 예외를 운영 fallback이나 다른 외부 서비스의 포괄 허용으로 확대하지 않는다. 테스트 데이터 범위는 추후 결정하며 현재 설정이나 구현 착수의 선행 조건으로 두지 않는다.
- Issue는 사람과 AI의 대화·판단·진행 창구다. PR은 해당 작업의 완결된 결과 문서와 변경 산출물이다.
- ADR과 Knowledge Base는 Git에 보관한다. 별도의 설계 Markdown 파일 작성은 필수가 아니다.
- Discussions는 사용하지 않는다. 사람의 대화·판단은 GitHub에서 수행하고, Agent에 내장한 사내 웹 현황판에서 repository·Issue별 진행과 실행 결과를 조회한다. 현황판은 같은 프로그램이 제공하며 별도 서비스나 DB를 요구하지 않는다.
- 대상 repository에는 기존 GitHub Actions가 없다. 이를 도입하거나 workflow를 이관하는 것을 요구하지 않는다.
- 기존 branch, 개발 절차, 빌드·테스트 스크립트, 개발 문서를 읽어 작업에 반영한다. 저장소 규칙을 다시 수동 작성하도록 강제하지 않는다.
- Agent는 GitHub App으로 GHES에 직접 연결되는 standalone 애플리케이션이다. 자체 실행 공간에 프로젝트를 clone하고 빌드·테스트한다. 별도 DB, Worker 서비스, 메시지 브로커, Actions Runner를 요구하지 않는다.
- 프로그램은 사내 호스트에서 이벤트를 기다리며 실행된다. 내부 HTTP 수신, 실행 대기열, 파일 상태 관리, 복구, 웹 화면과 조회 API는 같은 프로그램의 모듈이다.
- 프로토타입을 운영 코드로 교체하는 접근을 전제로 하지 않는다. 처음부터 최종 코드베이스에 상태 복구, 권한, 테스트, 배포·운영 절차를 구현한다.

## 2. 운영 구조

GitHub App을 인증·권한·이벤트 연동에 사용한다. Agent 자체는 사내 호스트에서 운영하고 GHES의 내부 webhook을 받는다. webhook 요청 안에서 긴 모델·빌드 작업을 끝내려 하지 않고, 검증·기록 후 내부 작업 대기열에 넘긴다. HTTP 기능은 webhook 수신과 인증된 사용자에게 제공하는 내장 웹 현황판·조회 API를 함께 담당한다.

```text
GHES Issue / 댓글 / PR / 리뷰
    -> GitHub App webhook
    -> Standalone Agent
         1. 출처·권한 확인, 이벤트 보존, 현재 요구사항·규칙·승인 확인
         2. 요구사항 정리 / 질문 / 설계 협의
         3. 승인·시작 조건 충족 시 작업별 clone 및 작업 브랜치 준비
         4. 코드·테스트·문서 수정
         5. 격리된 실행 환경에서 직접 빌드·테스트
         6. 검증한 변경을 push하고 완결된 PR 작성
         7. 정책에 따른 머지·직접 배포·배포 후 검증
    -> GitHub API로 Issue / PR / 검사·상태 결과 반영

Agent 내부 저장: 미처리 이벤트, checkpoint, 실행 로그, 산출물
Agent 운영 연결: 승인된 사내 GHES, 모델 API, 패키지·SDK 공급원, 배포 대상
명시적 테스트 프로파일: 허용 목록에 지정한 GPT API·사외 Maven; 테스트 자료 범위는 추후 결정

사내 사용자 브라우저
    -> Agent 내장 웹 화면 / 인증된 조회 API·상태 스트림
    -> 권한이 있는 repository / Issue / 실행의 현재 상태·이력 조회
    -> 질문 답변·승인·리뷰는 해당 GitHub Issue / PR 링크로 이동
```

요구사항 분석에 코드가 필요하면 구현 전에 읽기 목적의 checkout을 만들 수 있다. 구현은 유효한 승인·시작 조건을 만족한 이후에만 수행한다.

승인 대기 중에는 해당 작업의 모델·빌드 프로세스를 유지하지 않는다. Agent 본체는 다음 이벤트를 기다리며 다른 작업을 처리한다. 프로세스를 재시작하면 로컬 기록과 GitHub의 실제 상태를 확인해 재개한다. 작업은 실행 전용 OS 계정·파일 권한·프로세스 제한·네트워크 통제로 분리하며 컨테이너를 사용하지 않는다. 디렉터리나 프로세스만 다르게 만드는 것으로 권한 격리가 충족된다고 보지 않는다.

단일 프로그램·배포 단위를 기준으로 하며, 단일 프로세스에서 모든 코드와 자격 증명을 공유하는 것을 뜻하지 않는다. 단일 운영 인스턴스가 상태 디렉터리를 소유한다. 다중 호스트 고가용성이 필수인 경우는 별도 상태 조정 설계가 필요하다.

### 2.1 구성 요소

| 구성 요소 | 책임 |
| --- | --- |
| GitHub 어댑터 | App 인증, webhook 확인, GHES 설정·권한 조회, Issue·PR·검사 결과 갱신 |
| 정책·단계 엔진 | 실행할 때 GitHub 근거로 상태 재구성, 승인 유효성·전이·취소·재개 판단 |
| 저장소 규칙 수집기 | 실제 보호 규칙, 문서, CODEOWNERS, 빌드·테스트 설정과 내부 참조 분석 |
| 모델 registry·router·어댑터 | 여러 모델 등록·목적별 선택, API 규격·기능 차이 분리와 session 관리 |
| 작업 공간·실행 모듈 | clone·checkout·작업 브랜치, 파일 탐색·수정, 격리된 빌드·테스트·도구 실행 |
| 검증·게시 모듈 | 검증 근거 확인, 변경 게시, PR 문서 작성, 지식 변경 연결 |
| 배포 어댑터 | 환경별 배포, 정상 판정, 허용된 복구 절차 |
| 파일 상태 모듈 | 미처리 이벤트·버전이 있는 JSON·작업 파일 보존, 출처·commit·정책 일치 검증 |
| 내부 실행 관리자 | 대기열, 자원·동시성 제한, timeout·취소, 결과 전달, 재시작 후 재조정 |
| 내장 웹 현황판 | 사용자 인증·repository별 조회 권한, 목록·상세·실행 이력·정제된 로그·현재 상태 표시 |

### 2.2 Agent 내부 역할

다음 기능은 같은 프로그램 안의 모듈이다. 별도 workflow나 서비스를 repository에 배포하도록 요구하지 않는다.

| 역할 | 주요 입력 | 수행 내용 |
| --- | --- | --- |
| 요구사항 처리 | Issue 등록·수정, 수동 요청 | 원문 보존, 정리·질문·분리안 작성 |
| 대화·판단 처리 | 사용자 댓글·명시적 지시 | 권한·대상 revision 확인, 답변 반영, 승인·시작·중단 처리 |
| 설계·구현 | 유효한 시작 조건, 수정 요청 | 설계 협의 또는 자동 진행, 코드·테스트·Draft PR 작성 |
| 검증·PR 정리 | 코드 변경, 직접 실행한 검사 결과, 리뷰 | 검증, 리뷰 반영, 완결된 PR 문서 작성 |
| 배포·운영 | 정책을 만족한 변경, 내부 운영 신호 | 내부 배포 도구 실행, 확인·복구, 후속 요구사항 연결 |
| 상태 정리·복구 | 프로그램 시작, 실행 완료, 재개 지시, 내부 주기 점검 | 불완전 실행·게시 확인, 현황 갱신, 필요한 재개 |

작업 단계 전이는 명시적인 프로그램 코드가 수행한다. LLM은 요구사항·설계·코드 등을 생성하고, 자신의 승인·권한·완료 여부를 결정하지 않는다.

### 2.3 상세 설계 D1의 구현 선택

- 공통 구현은 CPython 3.12 계열, 애플리케이션 전용 runtime을 포함하는 RHEL별 설치 묶음으로 정한다. 정확한 patch·OpenSSL·native library 버전과 라이선스·내부 공급 가능성은 실제 빌드 검증 후 고정한다. RHEL 7·8·9 호환성을 검증한 상태는 아니며 시스템 Python으로 자동 대체하지 않는다.
- FastAPI + Uvicorn 단일 worker로 App webhook, 웹 화면, 인증된 조회 API·SSE를 제공한다. 별도 웹 서비스나 다중 scheduler를 만들지 않는다.
- Jinja2 서버 템플릿과 번들 CSS·JavaScript를 사용한다. 외부 CDN이나 별도 frontend 서버를 요구하지 않는다.
- 설정은 JSON으로 작성한다. 첫 핵심 모듈은 외부 패키지 없이 strict 검증 함수·immutable dataclass로 구현하며, 후속 HTTP 경계에 Pydantic 2 계열을 적용한다.
- HTTPX를 공통 통신 기반, PyJWT·cryptography를 App 인증 기반으로 사용한다.
- 명시적 Python 상태 전이·asyncio scheduler·제한된 tool loop를 구현한다. OpenHands·LangGraph·LangChain은 D1 필수 의존성에서 제외한다.
- 표준 파일 입출력·JSON journal/snapshot으로 상태를 보존한다. SQLite를 포함한 별도 DB는 사용하지 않는다.
- 현재 unittest로 설정·라우팅·평가 CLI를 검증하며, 후속 pytest 환경에서 상태 전이·어댑터·복구·통합·인수 시나리오를 추가한다.

세부 인터페이스와 검증 전제는 DETAILED_DESIGN.md에 정의한다. 선택한 라이브러리의 최신 상태·정확한 버전·OS 호환성은 실제 내부 설치 시험 전 지원 완료를 주장하지 않는다. 운영 설치·검증에는 승인된 내부 공급원만 사용한다. 외부 테스트 예외의 연결 범위는 2.8절에 한정한다.

사내 Maven 저장소를 Java 의존성과 Agent 설치 패키지의 내부 공급 경로로 사용한다. Agent 런타임·라이브러리·웹 자산을 검증된 설치 묶음으로 준비해 승인된 artifact 방식으로 반입한다. Maven 저장소를 Python package index로 취급하거나 pip가 임의로 외부 의존성을 찾게 하지 않는다. C# SDK·NuGet 공급은 후속 지원 시 확인한다.

### 2.4 직접 clone·빌드·테스트

1. 신뢰된 GHES metadata와 허용된 repository 범위로 clone 대상을 결정한다. 댓글에 적힌 임의 URL을 그대로 사용하지 않는다.
2. 대상 기본·작업 브랜치 규칙과 기준 commit을 확인하고, Issue별 분리된 작업 공간과 작업 브랜치를 준비한다.
3. README·개발 지침·프로젝트 설정·기존 스크립트에서 Java 빌드·테스트 방법을 읽는다. Maven·JUnit 경로를 우선 구현하며 실제 repository의 도구·명령을 확인한다. 다른 도구가 필요한 경우 그 사실을 알리고 지원 여부를 확인한다. 명령이 불명확하면 실행 계획을 제안해 필요한 정보만 확인한다.
4. OS·SDK·컴파일러·시스템 의존성·테스트 fixture와 내부 패키지 공급원을 repository 실행 프로필로 정한다. 빌드 방법이 없다는 이유로 검증을 통과시켰다고 보고하지 않는다.
5. 필요한 경우 변경 전 기준 테스트를 실행해 기존 실패와 새 실패를 구분한다. 승인 범위에서 구현·테스트를 수정한다.
6. 격리된 실행 환경에서 지정된 빌드·테스트·검사를 수행하고 명령, 환경, 종료 코드, 결과, 제한 사항을 기록한다.
7. 검증 대상 source tree와 commit을 일치시킨다. 검증 후 의미 있는 코드 변경이 생기면 영향을 받는 검사를 다시 수행한다.
8. 권한이 분리된 게시 기능으로 push·PR 작성 및 지원되는 GitHub 검사·상태 게시를 수행한다. 실제 실행 근거가 있는 결과만 게시한다.

clone의 submodule·LFS, dependency 설치, 빌드·테스트 코드가 사용하는 URL까지 선택한 환경의 목적지 정책에 포함한다. 운영은 내부 목적지만 허용하고, 명시적 외부 테스트는 설정된 GPT·Maven 목적지만 추가 허용한다. 허용되지 않은 remote를 자동 추적하거나 public registry fallback을 허용하지 않는다. 필요한 의존성이 없으면 이를 보고하고 기다린다.

기존 보호 규칙은 계속 준수한다. Agent가 게시할 검사 결과와 branch의 필수 검사 정책은 운영 설정에서 명시적으로 맞추며, 보호 규칙을 임의로 완화하지 않는다. GitHub의 실제 API 지원 범위는 사내 GHES에서 확인한다.

### 2.5 확인된 대상 환경에 대한 실행 방식

Java 8 프로젝트:

- 프로젝트 문서와 pom.xml 등에서 실제 빌드 방식, Maven·JDK 버전, profile, module, 테스트 실행 설정을 읽는다. 모든 Java 프로젝트가 동일한 빌드 명령을 쓴다고 가정하지 않는다.
- 빌드·실행에 사용할 사내 승인 JDK·Maven을 명시한다. Agent 런타임과 대상 프로젝트의 JDK를 분리하고 작업별 환경 변수를 구성한다.
- 운영에서는 의존성과 Maven plugin, Maven Wrapper를 사용하는 경우의 배포 파일도 승인된 내부 공급원으로 제한한다. 외부 테스트는 선택된 Maven 목적지를 settings.xml에 명시적으로 적용한다. mirror·인증과 pom.xml의 repository 선언을 함께 확인하고 설정 밖 fallback을 허용하지 않는다.
- 작업별 로컬 Maven cache 또는 권한·동시 쓰기가 통제된 cache를 사용한다. Maven 인증을 모델 입력·명령 로그에 노출하지 않는다.
- JUnit 버전과 실행 plugin·profile에 맞춰 unit test를 실행한다. 테스트 보고서의 실행·실패·오류·skip 수를 commit과 연결하고, 테스트가 실행되지 않은 경우를 통과로 표시하지 않는다.
- 필요한 fixture·환경 변수·내부 서비스 의존성을 확인한다. unit test 중심이라는 사실만으로 모든 테스트가 외부 자원을 사용하지 않는다고 가정하지 않는다.

C# 후속 지원:

- 현재 실행 지원 범위에는 포함하지 않는다. C# 프로젝트가 입력되면 미지원임을 명시하고 Java 검증 결과로 대신 성공 처리하지 않는다.
- 후속 도입 시 target framework, SDK·MSBuild 요구, Windows 전용 참조·UI·native 의존성을 확인해 별도 실행 어댑터를 정한다.
- 기존 Java 실행 경로와 모델·GitHub·웹 모듈을 변경하지 않고 프로젝트 실행 어댑터를 추가할 수 있게 한다. Windows 실행 환경을 현재 구성에 미리 추가하지 않는다.

RHEL·컨테이너 없는 실행:

- RHEL 7·8·9 각각에서 지원을 검증한다. 가장 낮은 지원 환경의 런타임·native 의존성 제약을 설계에 반영하되 다른 두 버전의 동작도 별도로 시험한다.
- 공통 코드베이스를 유지하며 필요한 경우 OS별 설치 묶음을 제공한다. 한 환경에서 만든 실행 파일·wheel을 모든 환경에 그대로 배포할 수 있다고 가정하지 않는다.
- 설치·시작, 사내 인증서 기반 GHES·Gemma4·Maven 통신, Java 빌드·JUnit, OS 계정 격리, 중단·재시작·웹 화면을 각 환경에서 검증한다. 운영 프로파일은 외부 의존성 다운로드 없이 실행되어야 한다.
- 코드 실행 계정은 App 키·Agent 상태·다른 작업 공간·배포 자격 증명에 접근하지 못하게 한다. 제어 프로그램의 환경 변수를 자식 프로세스에 그대로 상속하지 않는다.
- OS 계정·권한·프로세스 자원 제한·네트워크 제어에 필요한 서버 설정 권한과 실제 제공 기능을 확인한다. 컨테이너를 쓰지 않는 조건에서 이 격리·외부 통신 통제를 실제로 검증한다.

### 2.6 여러 LLM 등록과 목적별 선택

- provider(주소·인증·경계), protocol adapter, model(식별자·기능·한도), route(목적별 선택)를 분리한다. 모델명을 근거로 OpenAI 호환 endpoint·인증 header·streaming·도구 호출 형식을 가정하지 않는다.
- 공통 모델 인터페이스는 대화·문맥·허용 도구를 입력받고 텍스트·구조화된 도구 요청·사용량·오류를 정규화한다. 제공되지 않는 기능이나 수치는 미지원·미확인으로 표시한다.
- native 도구 호출이 있으면 해당 규격을 변환한다. 없다면 구조화된 작업 요청 출력을 활용할 수 있는지 대표 Java 작업으로 검증한 뒤 실행 방식을 결정한다. 모델의 임의 문자열을 shell 명령으로 바로 실행하지 않는다.
- 도구 입력 검증, 권한·내부 목적지 검사, timeout·중단, 테스트 결과 수집은 모델 밖의 프로그램 코드가 수행한다.
- 인증과 API 주소는 내부 설정으로 주입하고 비밀값을 로그·웹·Issue에 남기지 않는다. 장애 시 다른 모델이나 외부 endpoint로 자동 전환하지 않는다.
- 이미 지원한 protocol의 모델은 registry 설정으로 추가하고, 새 protocol만 adapter 코드로 추가한다. 모든 목적에 같은 모델을 쓰거나 요구사항·설계·구현·테스트 작성·리뷰·요약·운영 분석별로 다르게 선택할 수 있다. 여러 모델의 직접 연동 완료를 현재 공통 설계/구성 작업의 조건으로 요구하지 않는다.

설정·선택 우선순위·세션 변경·누적 예산·사람 리뷰와의 구분은 MULTI_MODEL_SPEC.md를 따른다. 다음의 실제 API 확인 절차는 후속 연결 시 참고용이며 지금은 수행하지 않는다.

API 규격이 아직 없는 상태의 진행 방법:

1. 사내에서 이미 사용 중인 호출 코드·SDK·curl 예제 또는 내부 API 문서를 확인한다. 실제 요청·응답 원본과 인증값은 사내에 보관한다.
2. 승인된 한 endpoint의 요청 경로, 인증 방식, 필수 header·body, 응답 텍스트와 도구 호출 구조를 확인한다. 모델 이름이나 버전에서 HTTP 규격을 역으로 추측하지 않는다.
3. 도구 정의 전달 → 모델의 도구 요청 → 프로그램의 무해한 테스트 도구 실행 → 도구 결과 반환 → 최종 응답의 왕복을 사내에서 검증한다. 단순 텍스트 응답 성공만으로 Agent 호환성을 판정하지 않는다.
4. 검증한 규격은 해당 protocol 어댑터와 비밀값을 제외한 계약 테스트에 반영한다. 요청과 응답은 공통 모델 인터페이스로 변환한다.
5. API 확인 전에는 규격과 독립적인 작업·승인·파일·빌드·웹 기능을 구현할 수 있다. 모델 경계의 테스트 대역은 단위·계약 시험에만 사용하며, 이를 실제 모델 연동이나 운영 완료로 보고하지 않는다.

Gemma4 연결 진단은 사용자가 설정한 승인된 내부 주소에서 수행한다. 여러 후보 endpoint를 추측해 탐색하지 않는다. 공통 Agent의 외부 GPT 시험은 별도의 명시적 테스트 프로파일에서 수행하고 Gemma4 연동 검증과 구분한다. 연결 확인 입력은 소스·업무 데이터가 없는 내용으로 한정하고 대상의 인증서·인증 방식을 따른다. 세부 확인 항목은 MODEL_API_CONTRACT.md에 기록한다.

### 2.7 사내 Maven 저장소를 통한 설치 패키지 배포

- Agent 애플리케이션, 승인된 런타임·native 의존성, 웹 자산, 실행·설치 스크립트, 라이선스 목록, 버전·검증 manifest를 설치 묶음으로 제공한다. 환경별 설정과 secret은 묶음에 포함하지 않는다.
- 같은 소스 릴리스에서 RHEL 7·8·9와 지원 CPU architecture에 맞는 설치 묶음을 준비한다. 예시 이름은 ai-dlc-agent-<version>-rhel7-<arch>.tar.gz이며 실제 artifact 형식·경로·권한은 사내 저장소 정책에 맞춘다.
- 사내 Maven 저장소의 정해진 artifact 좌표·버전으로 공급하고 무결성을 확인한 뒤 설치한다. 특정 artifact 형식을 저장할 수 있다는 것과 설치 대상 OS에서 실행된다는 것을 별도로 검증한다.
- 운영 설치 시 public PyPI·Maven Central·OS package mirror·CDN에 접속하지 않는다. 외부 테스트 프로파일의 Maven 허용을 Agent 설치 의존성 전체의 외부 다운로드 허용으로 확대하지 않는다. 필요한 파일이나 기본 OS 전제 조건이 없으면 누락 사항을 알리고 중단한다.
- Agent와 대상 Java 프로젝트의 JDK·Maven 설치 경로를 분리한다. Agent 설치가 시스템 Python이나 다른 업무 프로그램의 런타임을 변경하지 않게 한다.
- 설치 묶음은 RHEL별 설치·실행·내부 TLS 통신·격리·Java 빌드·JUnit·웹·재시작 시험으로 검증한다. 한 OS에서 만든 환경 디렉터리를 단순 복사한 것을 호환성 검증으로 간주하지 않는다.
- 업데이트는 명시적으로 선택한 내부 릴리스로 수행한다. 외부 업데이트 확인을 넣지 않는다. 교체 중 작업 처리, checkpoint 호환성, 이전 릴리스로 복구하는 절차를 함께 검증한다.

### 2.8 운영·외부 테스트 프로파일

여러 모델 provider·API 주소·model registry·목적별 route와 Maven 저장소를 설정 파일로 선택한다. 연결 schema_version은 2이며 예시는 config/production.example.json과 config/test-external.example.json에 둔다. 연결/repository 설정 로더와 모델 선택은 구현했으며 실제 모델·Maven 실행과 전체 Agent 흐름은 후속 범위다.

| 항목 | 운영 프로파일 | 명시적 외부 테스트 프로파일 |
| --- | --- | --- |
| 모델 | 등록된 사내 Gemma4·gpt-oss·DeepSeek·Gauss 등 | 등록된 GPT 테스트 모델 |
| Maven | 사내 저장소 | 명시적으로 지정한 사외 저장소 |
| 외부 연결 | 금지 | 설정된 GPT·Maven 목적지만 허용 |
| 입력 자료 | 허용된 사내 repository·문서 | 테스트 데이터 범위는 추후 결정 |
| 상태·작업 공간 | 운영 전용 | 별도 테스트 디렉터리·cache·로그·자격 증명 |
| 자동 전환 | 없음 | 없음 |

- 운영의 기본 정책은 외부 연결 금지다. production과 external_access=true의 조합은 실행 전 거부한다.
- 외부 테스트는 profile=test, 외부 연결 명시 허용, 목적지 목록 일치 조건을 만족해야 한다. 테스트 데이터 분류·허용 정책은 현재 설정에 추가하지 않는다.
- 설정은 Agent 운영자가 관리하는 위치에서 읽는다. 작업 repository나 모델 출력이 profile·목적지·인증 설정을 수정해 통제를 완화할 수 없게 한다.
- API 키는 환경 변수·기존 secret 수단으로 주입하고 설정 파일에는 참조 이름만 기록한다. GPT 모델 ID도 설정으로 지정하며 현재 모델을 추측해 하드코딩하지 않는다.
- Maven 선택은 실행용 settings의 mirror와 인증에 반영한다. 외부 테스트라도 wrapper·plugin·script·redirect가 임의 외부 주소를 호출할 수 있게 하지 않는다.
- 운영과 테스트의 clone·state·log·Maven cache·secret을 프로파일별로 분리한다. 실제 테스트에 사용할 데이터 범위는 별도 결정 사항으로 남긴다.
- 현재 profile·모델 provider·Maven 대상과 설정 revision을 실행 기록·웹 화면에서 확인할 수 있게 한다. 비밀값은 제외한다.
- GPT 시험으로 검증한 공통 동작과 실제 사내 Gemma4·Maven·SSO·RHEL 검증을 구분한다. 외부 테스트만으로 운영 도입 완료로 판단하지 않는다.

config/README.md에 설정 의미와 로더·실행기의 검증 계약을 기록한다. 이번 변경은 설정 예시와 설계이며 실제 외부 API 호출이나 다운로드는 수행하지 않았다.

## 3. 작업과 문서 모델

### 3.1 원문과 요구사항

- 최초 요청의 본문, 출처, 작성자, 접수 시점, 원문 revision을 보존한다. 이후 사람이 수정해도 이전 접수본을 덮어쓰지 않는다.
- AI 정리본은 목적, 범위, 인수 조건, 원문에 명시된 사실, AI 제안, 미확인 사항을 구분한다.
- 요구사항 승인 대상은 특정 정리본 revision과 작업 분리안이다. 일반 댓글이나 편집을 승인으로 추정하지 않는다.
- 요구사항 승인은 공통 필수 단계다. 승인과 개발 시작은 정책에 따라 별도로 처리할 수 있다.

### 3.2 Issue 관계

- 하나의 실행 Issue에 하나의 PR을 대응시킨다. Draft에서 검토·확정까지 같은 PR을 발전시킨다.
- 여러 독립 작업이 필요하면 상위 Issue와 실행 Issue로 나눈다. 상위 관리 Issue는 직접 구현 PR을 갖지 않는다.
- 각 실행 Issue는 범위, 인수 조건, 의존 관계를 가진다.
- 상위 요구사항 승인에 하위 작업의 범위·인수 조건을 포함하고, 승인 범위 내 실행의 중복 승인은 저장소 정책으로 정한다.
- 개발 중 분리가 필요해지면 작업 구조와 기존 변경의 귀속을 먼저 정리한다. 동일 변경을 여러 PR에 중복 게시하지 않는다.
- 상위 Issue는 하위 결과와 전체 인수 조건, 필요한 통합·배포 검증을 만족한 뒤 완료한다.

### 3.3 PR 문서

리뷰 요청 시 PR은 Issue의 전체 대화를 읽지 않아도 이해하고 판단할 수 있어야 한다.

필수 내용은 목적·범위·인수 조건, 최종 설계, 주요 판단 이유, 실제 구현, 검증 근거, 해당되는 배포·롤백 계획, 관련 ADR·Knowledge Base다. 작업 규모에 비례해 작성하되 빈 템플릿이나 실제로 수행하지 않은 검증을 완료 근거로 사용하지 않는다.

Draft PR에서는 미완성 내용을 표시하고 협업한다. 리뷰 중 변경되면 PR 본문도 갱신한다. 검증·승인은 해당 요구사항 revision, 설계 revision, PR head commit과 연결한다. 머지 시점의 문서 내용도 보존하고 GitHub에서 참조할 수 있게 한다.

PR에는 배포 전 검증 결과와 배포 계획을 기록한다. 실제 배포 결과·배포 후 검증은 Issue에 연결한다. 배포 대상 작업의 완료를 PR 머지와 동일시하지 않는다.

### 3.4 ADR과 Knowledge Base

- ADR은 중요한 결정 당시의 배경·대안·선택·영향을 보존한다. 결정을 바꾸면 대체 관계를 기록한다.
- Knowledge Base는 현재 유효한 개발·업무·운영 지식을 유지한다.
- Agent의 발견은 근거와 적용 범위를 갖춘 변경 제안으로 제출한다. 가설을 자동으로 확정 지식에 편입하지 않는다.
- 두 산출물 모두 Git의 검토·반영 절차를 거친다. 검색 인덱스를 추가해도 기준 원본은 Git이다.

### 3.5 DB 없는 상태 보존

| 정보 | 보존 위치·원칙 |
| --- | --- |
| 요구사항 원문·정리본·설계 합의 | Issue의 별도 Agent 기록으로 revision·내용·근거를 보존. 기존 기록은 자동 덮어쓰기하지 않음 |
| 사람의 판단 | 실제 사용자 댓글·PR 리뷰와 연결한 처리 기록. 현재 권한과 원본 수정 여부를 다시 확인 |
| 현재 단계·다음 행동 | Agent가 관리하는 상태 댓글·라벨. 재계산 가능한 표시이며 단독 승인 근거가 아님 |
| 코드·ADR·Knowledge Base | 작업 브랜치·PR·머지된 Git commit |
| 실행 상태·검사 결과 | Agent 실행 ID·commit에 연결한 로컬 기록과 GitHub에 게시한 검증 근거 |
| 이어서 사용할 작업 문맥 | 보호된 영속 디스크의 버전이 있는 JSON·작업 파일 |
| 최종 PR 문서 기록 | 머지 시점 본문 revision을 GitHub의 별도 기록으로 보존하고 Issue에 연결 |

현재 상태는 신뢰할 수 있는 기록의 출처, revision, commit, 실제 실행 결과를 확인해 재구성한다. AI가 긴 댓글을 읽고 임의로 완료·승인을 추정하지 않는다. Agent 기록의 작성 주체도 확인하며, 사용자가 같은 표시 형식을 복사한 댓글을 제어 기록으로 신뢰하지 않는다.

GitHub 댓글도 편집·삭제될 수 있으므로 불변 저장소라고 가정하지 않는다. 기록의 digest·출처를 검증하고, 필수 근거가 사라지거나 변경되면 불명확한 승인으로 계속 실행하지 않는다. 기록 크기는 GHES 제약 안에서 분할하고 revision 관계를 유지한다.

checkpoint에는 schema version, repository·Issue·task ID, 단계, 요구사항·설계 revision, 기준·작업 commit, 정책 revision, 실행·산출물 식별자를 포함한다. 비밀정보를 넣지 않는다. 파일 이름이 같다는 이유로 다른 실행의 결과를 사용하지 않는다.

프로그램이나 호스트가 재시작해도 남는 상태 디렉터리를 사용한다. 작업 코드를 실행하는 계정은 이 디렉터리에 접근할 수 없어야 한다. 파일 갱신의 원자성·잠금·검증, 백업·복원, 디스크 부족·기록 손상 처리를 구현하고 시험한다.

승인·요구사항 등 핵심 기록은 Issue·PR·GitHub에도 남긴다. 로컬 상세 로그·실행 파일의 보존·삭제 정책은 운영 기준으로 정한다. checkpoint가 없거나 손상된 경우 확인 가능한 Git commit·기록에서 재시작하거나 필요한 판단을 받는다. 복구되지 않은 중간 도구 실행을 완료로 처리하지 않는다.

## 4. 상태와 사람의 개입

### 4.1 단계와 상태의 분리

단계: 접수, 요구사항 정리, 요구사항 승인, 설계, 구현, 검증, PR 검토, 머지, 배포, 배포 후 검증, 완료.

상태: 실행 대기, 실행 중, 사람 응답 대기, 선행 작업 대기, 실패, 일시 중지, 취소, 완료.

각 작업은 다음 행동, 담당 역할, 대기 이유, 상태 변경 시점, 관련 PR·Agent 실행 ID를 가진다. 결과 게시 상태는 개발 작업 상태와 구분한다. 댓글 게시 실패가 테스트 재실행이나 배포 재수행을 유발해서는 안 된다. 로컬 작업 기록과 GitHub의 실제 근거를 대조하며 별도 상태 서버를 두지 않는다.

### 4.2 정책

- 요구사항 승인 후 자동 진행 또는 설계 협의를 선택한다.
- 설계 협의 모드에서는 제안·질문·수정·합의 후 구현한다.
- 자동 모드도 설정된 승인 관문과 예외 처리 규칙을 따른다. 머지·배포 자동화 여부는 별도 정책이다.
- 실행 중 수정·중단을 요청할 수 있다. 안전한 중단 지점에서 정지하고 영향 분석 후 재개한다.
- 취소 요청과 실제 실행 중단을 구분한다. 외부 효과가 있는 단계는 중단 가능 여부를 확인하고 결과를 재조정한다.
- 범위·인수 조건·주요 설계 변경은 관련 승인과 검증을 무효화할 수 있다. 어떤 관문으로 돌아갈지 명시적으로 결정한다.
- 사람의 권한, 승인 대상 revision, 유효 기간 또는 조건, 취소 이력을 기록한다.
- 조직 필수 정책과 실제 GitHub 보호 규칙을 모두 충족한다. 저장소별 선택으로 이 제한을 완화하지 않는다.

### 4.3 GitHub 표시

Issue의 Agent 관리 영역에는 현재 단계·상태·다음 행동·합의·미결 질문·근거 링크·마지막 갱신을 표시한다. 사람의 원문과 기존 댓글은 보존한다.

전체 repository·Issue 현황은 내장 웹 화면에서 확인한다. 각 GitHub Issue에는 핵심 상태를 계속 반영하고 웹 상세 화면을 연결할 수 있다. Projects·전용 현황 Issue는 선택 사항이며 웹 화면과 별도의 상태 기준을 만들지 않는다. 요구사항과 실행 작업 수는 구분한다. 근거 없는 진행률 대신 단계와 다음 행동을 표시한다.

### 4.4 내장 웹 현황판

웹은 Agent가 관리하는 작업을 관측하는 창구다. 요구사항 수정·질의응답·승인·리뷰의 기준 창구는 GitHub로 유지한다. 이번 요구는 조회 기능이며, 웹에서 작업 실행·승인·취소·재시도를 수행하는 기능은 자동으로 추가하지 않는다.

| 화면 | 표시 내용 | 이동·필터 |
| --- | --- | --- |
| 전체 현황 | 접근 가능한 repository별 진행·사람 대기·실패 수, 최근 활동, 내 판단이 필요한 작업 | repository·상태·담당자별 필터 |
| repository 상세 | Issue별 제목, 상·하위 관계, 단계·상태, 현재 작업, 다음 행동·담당자, 상태 경과 시간, PR | Issue·PR 링크, 단계·상태·검색 필터 |
| Issue 상세 | 원문·승인본 링크, 선택한 진행 정책, 단계 흐름, 실행 이력, 미결 질문·대기 이유, 코드·검증·배포 결과 | GitHub 답변·승인·리뷰 위치로 이동 |
| 실행 상세 | 실행 ID·시도 번호, 기준 commit, 시작·종료 시각, 실제 도구·빌드·테스트 결과, 정제된 로그, 실패와 복구 상태 | 단계별 로그·결과·연관 실행 탐색 |

목록의 예시 필드는 repository, Issue 번호·제목, 단계, 상태, 현재 수행 작업, 다음 행동·담당자, PR, 마지막 갱신이다. 부모 Issue와 실행 Issue는 계층으로 보여주고 집계에서 중복 계산하지 않는다. 완료된 하위 작업 수는 표현할 수 있지만 이를 전체 작업의 예상 완료 비율로 해석하지 않는다.

현재 작업 표시는 코드·도구 실행 이벤트에서 만든다. 예: 관련 파일 탐색, 설계안 게시, 특정 파일 수정, 빌드 실행, 테스트 결과 확인. 모델의 비공개 추론을 노출하거나 모델이 말한 완료 상태를 실행 근거 없이 그대로 표시하지 않는다.

상태 표시 원칙:

- 단계와 상태, 현재 동작과 다음 사람 행동을 분리한다. 빌드 실패 후 수정 중과 실패로 중단된 작업을 구분한다.
- 실패한 시도와 후속 성공을 각각 보존한다. 다른 commit에서 수행한 테스트를 현재 commit의 성공 근거로 표시하지 않는다.
- 웹과 GitHub 요약은 같은 작업 상태를 사용한다. GitHub 게시가 지연되면 실제 실행 상태와 게시 지연을 구분해 표시한다.
- 전체 작업의 근거 없는 완료 퍼센트나 예상 시간을 만들지 않는다. 테스트 20/30처럼 측정 가능한 하위 진행만 표시한다.
- 최신 갱신 시각을 표시하고 연결이 끊기면 마지막으로 확인된 상태임을 알린다. 화면 정지가 작업 정지로 오인되지 않도록 한다.

갱신 방식은 초기 상태 조회 후 SSE 등 서버에서 브라우저로 상태를 보내는 방식을 우선 검토한다. 내부 프록시 환경에서 스트림을 지원하기 어려우면 제한된 주기 조회를 사용할 수 있다. 연결 복구 시 revision·이벤트 식별자를 확인하고, 필요한 기록이 없으면 최신 snapshot을 다시 읽는다. 매 화면 갱신마다 모델을 호출하거나 전체 로그 파일을 다시 읽지 않는다.

웹용 조회 모델은 기존 작업 파일·실행 이벤트에서 만들고 메모리에 색인할 수 있다. 이 색인은 재시작 시 재구성 가능하며 별도 DB를 만들지 않는다. 목록은 페이지 단위로, 큰 로그는 범위를 제한해 읽는다. 느린 브라우저 연결이 Agent 작업 실행을 막지 않게 처리한다.

### 4.5 웹 인증과 데이터 경계

- GHES의 SSO 기반 로그인이 확인되었다. 대시보드는 GHES 사용자 인증과 연결하는 방식을 우선 검토하며, 지원되는 인증 흐름을 확인한 뒤 구현한다. GitHub 로그인 세션이 별도 호스트의 대시보드에 자동 공유된다고 가정하지 않는다. App 설치 권한은 브라우저 사용자의 조회 권한과 동일하지 않다.
- 화면·API·상태 스트림·로그·다운로드 모두 사용자별 repository 접근 범위를 확인한다. 전체 집계도 접근 가능한 repository만 포함한다.
- 로그·오류·도구 입력에서 비밀정보를 제거한다. 상태 디렉터리나 임의 파일 경로를 정적 웹 경로로 노출하지 않는다.
- Issue 본문·Markdown·실행 로그는 신뢰되지 않은 콘텐츠로 취급한다. HTML·스크립트 실행과 임의 이미지·미디어의 자동 로드를 막는다.
- 브라우저가 public CDN·폰트·분석·오류 보고 서비스에 접속하지 않게 한다. 정적 자산은 Agent가 내부에서 제공하고, CSP 등 브라우저 정책을 적용해 승인되지 않은 외부 요청을 제한한다.
- 웹 화면에는 App 개인 키·모델 API 키·배포 자격 증명을 전달하지 않는다. 브라우저는 인증된 같은 프로그램의 조회 API를 사용한다.

## 5. 저장소 규칙과 실행 프로필

AI-DLC 흐름은 언어 독립적이며 repository별 실행 환경·명령·결과 해석을 공통 실행기에 주입한다. Java/Maven/JUnit은 초기 검증 대상과 도구 보조 기능이지 모든 repository에 강제할 아키텍처가 아니다. 이 Agent의 Python 소스도 같은 작업 흐름에 별도 실행 profile로 적용한다. 설정 검증은 java_maven과 언어 중립적인 command를 지원한다. 공통 command 실행·중단·복구·JUnit 검증은 구현했고 운영 runtime과 실제 repository 연결은 후속 대상이다.

읽을 대상:

- GitHub의 기본 브랜치, 접근 가능한 보호 규칙, 필수 리뷰·검사, 머지·배포 조건.
- CONTRIBUTING.md, AGENTS.md, CODEOWNERS, PR 템플릿, 브랜치·릴리스 문서.
- 빌드·테스트·배포 스크립트, 프로젝트 설정 파일, 의존성 잠금 파일, 내부 SDK·패키지 공급 설정.

조회 시점·파일 경로·commit과 함께 실행 프로필을 만든다. 프로필은 원본 규칙의 사본을 새 정책으로 고정하는 것이 아니라 실행 계획의 근거다.

조회 실패를 규칙 없음으로 해석하지 않는다. 문서와 실제 제한이 충돌하면 제한을 준수하고 미확인 사항을 알린다. 동적 스크립트의 모든 동작을 정적 분석으로 증명할 수 있다고 가정하지 않는다.

작업 시작과 주요 실행 직전에 필요한 규칙을 확인한다. 해당 작업의 설정·정책 revision도 기록한다. 읽기 권한과 변경 권한을 구분하고 Agent가 자신의 통제를 완화할 수 없게 한다.

## 6. 운영 신뢰성

### 6.1 이벤트와 작업

- webhook의 서명·출처를 확인하고, 처리할 이벤트를 지속 저장한 뒤 수신 완료를 응답한다. 모델·빌드 실행은 수신 요청에서 분리한다.
- 실행마다 출처 이벤트, task ID, 대상 revision, 실행 ID와 attempt를 기록한다.
- 로컬 파일·GitHub 갱신·배포 호출 전체를 묶는 원자적 트랜잭션은 없다고 전제한다. intent·결과의 상관관계와 실제 조회를 통해 부분 실패를 복구한다.
- 모든 이벤트가 한 번만 또는 순서대로 도착한다고 가정하지 않는다. 현재 Issue·PR·승인 근거를 다시 읽고 이미 처리된 작업인지 확인한다.
- 변경 게시·PR 생성·배포처럼 효과가 남는 작업은 재시도 전에 수행 여부를 조회한다. 하나의 식별 가능한 작업을 중복 생성하지 않도록 한다.
- 실행이 끝났는데 표시 갱신이 실패한 경우 결과 반영만 재시도한다. 프로그램 시작 및 내부 주기 점검에서 불완전 기록을 조회한다.
- App이 갱신한 상태 댓글을 새로운 사용자 요구사항으로 처리하지 않는다.
- 중단 중 전달되지 않은 webhook이 자동으로 다시 온다고 가정하지 않는다. 지원되는 GHES 전달 이력·재전송과 범위가 제한된 API 재조회를 확인한다. 재구성할 수 없는 원문을 복구됐다고 표시하지 않는다.

### 6.2 동시 실행

- 단일 Agent 인스턴스가 상태 디렉터리를 소유한다. repository와 Issue별 상태를 분리하고 같은 Issue의 변경 작업은 하나만 활성화한다.
- 이벤트 파일과 작업 식별자로 미처리 작업을 관리한다. 승인·중단·수정의 순서와 현재 유효성을 확인하고 재시작 후 처리할 작업을 재구성한다.
- 작업별 clone·브랜치·빌드 출력·임시 디렉터리를 분리하고 repository별 동시 실행 수와 CPU·메모리·디스크 사용량을 제한한다.
- 선행 작업과 기준 브랜치 변경을 확인한다. 영향받은 검증은 최신 실행 대상에서 다시 수행한다.
- 같은 환경에 대한 충돌 가능한 배포는 프로그램의 환경별 잠금이나 기존 배포 도구의 직렬화 기능으로 제어한다.
- 검증한 산출물과 배포할 산출물의 commit·digest 관계를 확인한다.

### 6.3 실패와 복구

- 재시도 가능한 오류와 사람 판단이 필요한 오류를 분류한다.
- 작업별 시간·호출·재시도 한도를 둔다. 무한 수정·테스트 반복을 허용하지 않는다.
- Agent나 작업 프로세스 중단 후에는 식별된 checkpoint 또는 GitHub의 확인 가능한 마지막 기록에서 재개한다. 저장되지 않은 메모리·파일 상태가 복구된다고 가정하지 않는다.
- checkpoint의 schema 변경과 Agent 버전 변경에 대한 호환성·마이그레이션을 검증한다. 호환되지 않으면 확인된 기준에서 재시작한다.
- 원문·요약·digest·검증 근거의 로컬 보존·백업·복원을 관리하고 GitHub 기록과 사내 GHES 보존 정책에 연결한다. 새로운 별도 저장 서비스를 기본 요구하지 않는다.
- 정책·앱·모델 어댑터 변경 이후에도 기존 작업을 식별하고 재개 가능 여부를 판단한다.

## 7. 실행 경계와 권한

- 제어 프로그램, 코드·빌드·테스트 실행 공간, 배포 실행 권한을 분리한다.
- 신뢰된 프로그램 코드와 정책으로 실행을 제어한다. repository의 코드·스크립트·설정이 제어 프로그램이나 자신의 권한을 임의로 변경하지 못하게 한다.
- Agent·테스트 실행 환경에 App 개인 키나 운영 배포 자격 증명을 제공하지 않는다. 분석·코딩·게시·배포의 자격 증명과 권한을 분리한다.
- 상태 전이는 신뢰된 제어 코드가 수행한다. 작업 결과의 실행·repository·commit·task·실행 환경 관계를 확인한다.
- 변경 게시와 PR 작성에는 범위가 제한된 인증을 사용한다. App의 설치 범위와 권한은 repository별로 제한한다.
- 모델 요청과 shell 실행의 자격 증명을 분리하고, 로그·댓글·모델 입력에 비밀정보가 포함되지 않도록 한다.
- 운영 도구 실행 정책에서 승인된 내부 대상만 허용한다. 외부 테스트 프로파일에는 명시된 GPT·Maven 목적지만 추가한다. 일반 외부 검색 도구, 자동 fallback, 자동 원격 plugin 로딩을 제공하지 않는다.
- 환경별 목적지 정책, 공급원 고정, 프로세스·네트워크 격리를 함께 적용한다. 공용 DNS·proxy·telemetry 등 설정 밖 외부 경로도 검증한다. 구성 파일만으로 임의 코드의 통신 차단이 완성됐다고 간주하지 않는다.
- 운영 차단 동작은 실제 사외 통신을 발생시키지 않는 시험으로 검증한다. 외부 GPT·Maven의 실제 연결 시험은 명시적으로 선택한 테스트 프로파일과 목적지 목록에 따라 별도로 수행한다. 테스트 자료 범위는 추후 정한다.

## 8. 운영·배포 범위

- 배포 대상, 방법, 승인 조건, 정상 판정, 실패 처리, rollback 조건을 repository와 환경별로 정의한다.
- 대상 애플리케이션에 DB 변경이 포함되는 경우, 그 데이터 호환성과 복구는 코드 rollback과 별도로 검토한다. Agent 자체에 DB를 도입한다는 의미는 아니다.
- 운영 신호 수집·분석·개선 Issue 생성과 실제 복구 실행의 권한을 구분한다.
- 정의된 조건과 검증 방법을 갖춘 복구 절차만 정책에 따라 자동 수행한다.
- 새로운 코드 변경이 필요한 운영 개선은 요구사항 정리·승인 흐름으로 연결한다.
- 공통 배포 정책과 repository·작업 단위 중단·재개 절차를 제공한다. 각 실행은 현재 정책을 확인하며, 이미 실행 중인 작업에는 자식 프로세스 종료·timeout 및 Agent의 중단 확인 지점이 필요하다. 진행 중 배포에 대한 중단 결과는 별도로 확인한다.

## 9. 검증과 운영 도입 기준

테스트 수가 아니라 동작과 복구 가능성으로 완료를 판정한다.

정량 평가와 운영 도입 판정은 [EVALUATION_SPEC.md](EVALUATION_SPEC.md)를 따른다. 실행 엔진·목적별 LLM·업무 성과·실제 전체 흐름을 나누어 평가하고, 중대 오류 관문과 업무 품질/실용성 관문을 모두 충족해야 한다. 수치 기준은 초기 제안이며 정식 평가 전에 고정한다. 평가 실행기와 보고서는 같은 패키지에 포함하고 별도 DB/서비스를 추가하지 않는다.

| 검증 범위 | 합격 조건 |
| --- | --- |
| 정상 전체 흐름 | 승인된 요구사항이 설계·구현·검증·완결된 PR·배포 후 확인까지 추적 가능 |
| 원문·버전 | 원문 수정 이후에도 기존 요청과 승인 대상 확인 가능 |
| 승인·권한 | 권한 없는 입력, 잘못된 revision, 취소된 승인으로 실행 불가 |
| 중복·역순 이벤트 | 중복 PR·배포·오래된 상태 덮어쓰기 없음 |
| 장애 복구 | Agent·작업 프로세스 중단, GitHub 부분 갱신 실패 이후 확인된 기록에서 재개 가능 |
| 동시성 | 여러 Issue와 환경별 배포의 충돌 통제 |
| 요구사항 변경 | 실행 중 수정이 영향 분석·기준 갱신·필요한 재승인으로 연결 |
| 실제 검증 근거 | 보고된 테스트·배포 결과와 실행 기록·commit·산출물이 일치 |
| Java 8·JUnit | 지정 JDK·Maven과 내부 의존성으로 재현 가능한 빌드·테스트 수행, 0건·skip·기존 실패 구분 |
| 컨테이너 없는 격리 | 작업 코드가 제어 상태·secret·다른 작업에 접근하지 못하고 승인되지 않은 통신이 실행 전에 거부됨 |
| OS 호환성 | RHEL 7·8·9 각각에서 승인된 런타임으로 시작·내부 통신·Java 빌드·JUnit·격리·복구·웹 동작 확인 |
| 모델·지원 범위 | 선택한 사내 모델·route로 Java 대상 전체 흐름 검증, C#·미연결 모델·미지원 도구를 성공으로 오표시하지 않음 |
| 내부 설치 패키지 | 사내 Maven의 고정 artifact로 RHEL별 설치·실행·업데이트·복구, 외부 의존성 조회·다운로드 없음 |
| 실패·복구 | 테스트 실패, 배포 실패, 복구 실패를 구분하고 허용된 복구 수행 |
| 연결 경계 | 운영은 내부 승인 대상만 접근, 외부 자동 연결 거부; 테스트는 명시된 목적지만 사용 |
| 프로파일 분리 | 운영에서 외부 설정 거부, 외부 테스트의 명시 선택, 작업·state·secret 분리와 자동 fallback 부재 확인 |
| 웹 현황 일치 | 실행·대기·실패·재시도·재시작이 repo·Issue 화면에 올바르게 표시되고 오래된 상태를 구분 |
| 웹 조회 권한 | 허용되지 않은 repository의 집계·상태·로그·스트림·다운로드 노출 없음 |
| 웹 외부 요청 차단 | UI 자산·렌더링·오류 보고를 포함해 브라우저가 승인되지 않은 외부 요청을 만들지 않음 |
| 지식 관리 | 근거 있는 ADR·Knowledge Base 변경과 이력 확인 |
| 운영 준비 | Agent 설치·업그레이드, checkpoint 호환성, 로컬·GitHub 기록 보존·복원, 관측·중단 절차 검증 |

사내 운영 담당자가 확정할 용량, 응답 시간, 가용성, 복구 시간·시점, 보존 기간을 시험 조건에 포함한다. 서로 다른 특성을 가진 대표 repository에서 검증한다. 전체 기준을 만족하기 전에는 운영 도입 완료로 선언하지 않는다.

## 10. 확인된 환경과 남은 질문

확인된 내용: RHEL 7·8·9 모두 지원 대상, 컨테이너 미사용, Java 8·JUnit을 현재 운영 도입 범위로 함, 여러 사내 모델 등록·목적별 선택, 모델별 API 상이, GHES SSO 로그인, 사내 Maven을 통한 의존성·설치 패키지 공급. 테스트에는 설정으로 GPT API·사외 Maven을 선택할 수 있다. C#은 후속 지원이며 Jira는 현재 범위 제외. 각 모델 도구 호출과 내부 webhook 연결은 실제 검증 전이다. 사내 종속 계약 확인은 사용자의 지시에 따라 현재 작업에서 진행하지 않는다.

사용자는 외부 테스트 데이터 범위를 추후 결정하기로 했다. 현재는 데이터 분류 설정을 두지 않고, 사내 운영 프로파일은 사내 LLM·Maven만 사용하도록 유지한다. 외부 호출·자료 전송은 이번 설정 작업에서 수행하지 않았다.

실제 provider의 인증·요청·응답·도구 호출 규격은 후속 연결 때 확인한다. 사용자에게 지금 규격을 요구하거나 확인을 반복하지 않는다. MODEL_API_CONTRACT.md의 절차는 이후 연결과 도구 결과 반환의 검증 기준이며, registry/router/session의 설계·구현은 독립적으로 진행할 수 있다.

이후 내부 연동·운영 검증에 필요한 정보:

- GHES 버전, App 등록·권한·사용자 인증 흐름, 실제 내부 webhook 연결과 인증서.
- 설치 묶음의 사내 Maven artifact 경로·형식·인증, 사용 가능한 런타임·CPU architecture, OS 계정·권한·네트워크 통제를 설정할 수 있는 범위.
- Java 프로젝트의 JDK·Maven·JUnit·plugin 버전, 빌드·테스트 명령과 내부 repository 설정.
- Gemma4의 context·호출 제한, 도구 호출·구조화 응답 동작과 허용된 내부 API 주소.
- 배포 대상·명령·정상 판정·복구 절차, 승인 역할, repository별 선택 정책.
- 영속 디스크·secret 관리·백업·보존, 예상 동시 작업 수·복구 목표.
- 웹의 repository별 조회 권한, HTTPS·내부 프록시·상태 스트림 지원 환경.

이 정보가 없는 부분은 가정으로 코드를 고정하지 않는다. standalone Agent의 핵심 도메인·단계 실행과 GitHub·모델·프로젝트 실행 어댑터를 분리해 확정된 동작부터 운영 품질로 구현하고, 실제 내부 환경에서 계약·통합·복구 시험을 완료한다. Agent 애플리케이션 하나 안에서 운영 요건을 구현하며 별도 DB·Worker 서비스나 Actions 도입을 요구하지 않는다.
