# 모델 API 연결 확인 — 후속 연동 절차

상태: 사내 모델 API 규격을 이후 확인할 때 사용할 절차. 사용자의 지시에 따라 현재 사내 계약 확인은 진행하지 않는다. 여러 모델 구성의 기준은 [MULTI_MODEL_SPEC.md](MULTI_MODEL_SPEC.md)이며, 아래 Gemma4 예시는 다른 provider에도 적용하는 후속 연결 기준이다. 실제 endpoint·인증값·API schema는 제공되지 않았고 연결 시험·외부 조회를 수행하지 않았다. 별도 외부 GPT 시험은 config/README.md를 따른다.

## 1. 목적과 경계

Gemma4라는 모델명과 서비스 API 규격을 구분한다. 사내 게이트웨이·서빙 구성에 따라 같은 모델도 요청·응답·인증 형식이 다를 수 있다. 모델명만으로 OpenAI 호환 API를 가정하지 않는다.

원본 호출 예제·로그·인증값·업무 데이터는 사내에 보관한다. 계약을 정리하는 데는 비밀값을 제거한 요청·응답 구조가 필요하다. 이 문서는 외부 채널로 내부 자료를 보내도록 요구하지 않는다.

## 2. 확인 자료의 우선순위

1. 사내에서 실제로 동작하는 client 코드·SDK·curl 호출 예제.
2. 해당 서비스의 내부 API 문서 또는 OpenAPI 명세.
3. 승인된 내부 개발 환경에서 확인한 비밀값 제거 요청·응답 예제.

호출 예제 하나로 기본 구조를 파악할 수 있지만, 텍스트 호출 예제만으로 도구 호출 호환성을 확정하지 않는다. 아래 왕복 흐름과 오류·제한 사항을 추가 확인한다. 자료 접근과 시험은 알려진 승인 대상만 사용하며 임의 URL·경로 탐색을 하지 않는다.

## 3. 확인할 계약

| 항목 | 확인 내용 |
| --- | --- |
| 대상 | 내부 host·path·HTTP method, 모델 식별 방법 |
| 인증 | header 이름·scheme·토큰 갱신 방식, 사내 CA·proxy 조건; 실제 비밀값은 별도 설정 |
| 기본 입력 | 대화 메시지의 role·content 형식, 필수 옵션, 이전 대화 전달 방식 |
| 기본 응답 | 텍스트 경로, 완료 상태, 사용량 제공 여부 |
| 도구 정의 | 이름·설명·입력 schema·선택 옵션의 전달 위치와 형식 |
| 도구 요청 | 호출 ID·이름·인자, 복수 호출 여부, 인자가 문자열인지 객체인지 |
| 도구 결과 반환 | 결과 메시지 role·호출 ID 연결·content 형식, 다음 요청에 포함할 대화 |
| 오류·제한 | HTTP·응답 오류 형식, 호출 제한, 입력 길이, timeout·재시도 조건 |
| streaming | 지원 여부와 chunk 형식; 첫 연결 검증은 가능한 경우 비 streaming부터 수행 |

client가 통신을 중단하는 것과 서버 측 추론이 취소되는 것을 구분한다. 실제 취소 기능이 있는 경우에만 원격 취소 성공을 보고한다.

## 4. 내부 연결 진단 절차

1. 승인된 endpoint와 모델·인증·CA 설정을 로컬로 제공한다. 비밀값은 진단 결과나 로그에 남기지 않는다.
2. 업무 데이터가 없는 짧은 입력으로 텍스트 응답을 확인하고 실제 schema를 기록한다.
3. 부작용 없는 로컬 도구 하나를 정의한다. 예: 입력 문자열을 그대로 반환하는 probe_echo. 파일·shell·Git·배포 도구는 이 시험에 제공하지 않는다.
4. 모델이 반환한 도구 호출의 ID·이름·입력 schema를 검증한다. 로컬 도구만 실행하고 결과를 제공된 API 규격으로 돌려준다.
5. 모델의 최종 응답을 확인한다. 호출 ID와 대화 연결, 종료 조건이 올바른지 검증한다.
6. 필요한 범위에서 복수 호출, 잘못된 인자, timeout·인증 오류 등의 처리를 검증한다. 오류 시험도 승인된 내부 환경·범위에서 수행한다.
7. 확인된 구조를 해당 protocol 어댑터와 비밀값을 제외한 계약 테스트에 반영한다. 확인되지 않은 기능은 미지원 또는 미확인으로 표시한다.

진단은 알려진 주소에 대해 명시적으로 실행한다. 실패 시 public provider, 추측한 다른 endpoint, 외부 모델 목록으로 fallback하지 않는다. 인증서 검증을 꺼서 연결 문제를 우회하지 않는다.

## 5. 어댑터 경계

```text
작업·승인·Agent 실행 엔진
    -> 공통 요청: 대화, 허용 도구, 실행 제한
    -> 선택한 model/provider와 protocol 어댑터
    -> 확인된 사내 API
    -> 공통 응답: 텍스트, 구조화된 도구 요청, 종료·오류·사용량
```

- 규격 변환은 어댑터 안에 한정한다. 인증·접속 정보는 설정으로 분리한다.
- 단순 필드·header 차이는 검증된 설정으로 표현할 수 있지만 임의 HTTP 프로토콜을 설정만으로 모두 지원한다고 주장하지 않는다. 복잡한 인증·streaming·대화 구조는 어댑터 코드로 구현한다.
- 모델이 도구를 요청해도 실제 실행 권한은 프로그램이 판단한다. 도구 이름·인자를 검증하고 승인·파일 범위·내부 목적지 제한을 적용한다.
- API 규격 확인 전 공통 기능은 테스트 대역으로 검증할 수 있다. 이 결과는 실제 Gemma4 연동 검증과 구분한다.
- 실제 Java 작업의 파일 탐색·수정·테스트 결과 해석까지 검증한 후 해당 model/provider와 코딩 실행 엔진의 호환성을 확정한다.

## 6. 완료 조건

- 알려진 사내 API에서 텍스트와 도구 왕복 호출 성공.
- 올바른 요청·응답 변환과 오류·제한·중단 처리 확인.
- 인증값·업무 데이터의 로그 유출 및 승인되지 않은 외부 호출 없음.
- 지원 RHEL 환경에서 사내 인증서·인증 설정으로 실행.
- 실제 Gemma4를 사용한 Java 작업 시나리오 검증. 테스트 대역만 통과한 상태를 운영 연결 완료로 표시하지 않음.
