# GitHub App 직접 연동 Standalone Agent 평가

평가일: 2026-09-20

상태: Actions 중심 설계에 대한 당시의 대안 평가 기록. 이후 사용자가 대상 repository에는 기존 Actions가 없다고 확인했으며, GitHub App 직접 연동과 Agent의 직접 clone·빌드·테스트를 기준으로 DESIGN.md를 갱신했다. 이 문서의 기존 Actions 이관·호환성 조건은 현재 대상 repository의 선행 조건이 아니다. 현재 설계는 DESIGN.md를 따른다. 외부 조회·접속 없이 작성했으며, 사내 GHES·모델·실행 환경에서의 검증은 아직 수행하지 않았다.

## 1. 결론

GitHub App으로 사내 GHES에 직접 연동하는 standalone Agent 프로그램 하나로 요구된 개발 흐름을 구현할 수 있다. Actions는 Agent 실행의 필수 구성 요소가 아니다.

단, GitHub App은 권한·인증·이벤트 연동을 제공하는 수단이며 프로그램을 호스팅하거나 빌드·테스트·배포를 대신 실행하는 서비스가 아니다. Agent를 실행할 사내 호스트, 격리된 작업 환경, 지속되는 상태 파일은 필요하다.

추천 후보는 단일 운영 인스턴스의 장기 실행 애플리케이션이다. 이벤트 수신, 작업 스케줄링, 상태 복구를 같은 프로그램의 모듈로 포함한다. 별도 DB 서비스, 상시 Worker 서비스, 메시지 브로커, 별도 사용자 웹 UI를 추가하지 않는다.

작업별 자식 프로세스·컨테이너는 격리를 위한 실행 환경이며 독립적인 업무 서비스로 운영하지 않는다. 단일 프로그램이라는 요구를 단일 프로세스에서 모든 사용자 코드를 실행한다는 의미로 해석하지 않는다.

## 2. 후보 구성

```text
사내 GitHub Enterprise Server
    -- GitHub App webhook -->
    사내 호스트의 Agent 애플리케이션
      - 내부 HTTP 이벤트 수신·출처 확인
      - 입력·승인·정책 검증
      - 파일 기반 미처리 이벤트·작업 기록
      - 실행 대기열과 repository/Issue별 동시성 관리
      - 요구사항·설계·구현·검증·배포 단계 실행
      - 재시작 후 실제 결과 확인 및 재개
      - GHES API를 통한 Issue·PR·검사 결과 갱신
          |
          +-- 사내 모델 API
          +-- 격리된 checkout·빌드·테스트 작업
          +-- 승인된 내부 배포 도구·대상
          +-- 보호된 로컬 상태·로그·산출물 디렉터리
```

사람은 계속 GitHub에서 대화·승인·리뷰·상태 확인을 한다. Agent의 이벤트 수신 endpoint는 사용자용 웹 화면이 아니며 GHES에서 접근 가능한 내부 주소에만 둔다.

FastAPI 같은 HTTP 라이브러리는 수신 모듈 구현 후보로 사용할 수 있지만 필수 제품은 아니다. App을 사용하는 것 자체가 반드시 webhook을 요구하는 것은 아니며 내부 API polling도 후보가 될 수 있다. 다만 polling에는 지연, 조회 부하, 조회 사이에 변경·삭제된 내용의 유실 가능성이 있어 원문 보존·대화 응답 요구에 맞춰 webhook 수신을 우선 검토한다.

이전 Actions 안은 실행 후 종료하는 CLI였고, 이번 안은 이벤트를 기다리는 프로그램이 계속 실행되는 구조다. 승인을 기다리는 개별 작업은 실행 자원을 점유하지 않는다. 프로세스 재시작 시 파일에 보존된 정보를 읽고 GitHub·배포 대상의 실제 상태를 확인한다.

## 3. 요구사항 충족 평가

| 요구사항 | 평가 | 필요한 구현·확인 |
| --- | --- | --- |
| 사내 GHES 연동 | 가능 | 내부 API 주소, App 설치·권한, 이벤트 종류 확인 |
| Gemma·Gauss 등 사내 모델 | 가능 | 모델 어댑터와 실제 도구 호출 호환성 검증 |
| 원문 보존·요구사항 정리 | 가능 | 수신 원문 revision 보존, 질문·분리안 게시 |
| 필수 요구사항 승인 | 가능 | 승인 주체·정리본·revision을 코드로 검증 |
| 자동 진행·설계 협의 선택 | 가능 | 명시적 단계 엔진과 중단·재개 정책 |
| Issue·PR 중심 협업 | 가능 | GitHub API를 통한 대화·결과·검사 게시 |
| 실행 Issue 하나에 PR 하나 | 가능 | 작업 식별·분리안·중복 PR 방지 |
| ADR·Knowledge Base를 Git에 관리 | 가능 | 격리 checkout에서 변경 후 PR 반영 |
| 여러 repository·Issue 동시 진행 | 가능 | 자원 제한, 작업 격리, Issue·환경별 잠금 |
| GitHub 안의 현황판 | 가능 | Issue 요약·라벨, 지원되는 경우 Projects 사용 |
| 별도 DB 없는 운영 | 조건부 가능 | 단일 인스턴스, 영속 파일, 기록 검증·복구 시험 |
| Actions 없는 빌드·테스트·배포 | 조건부 가능 | 동일 작업을 수행할 독립 명령·도구·권한·환경 필요 |
| 사외 통신 시도 금지 | 환경과 구현 검증 필요 | 내부 목적지 고정, 도구 제약, 격리 실행, 의존성·telemetry 검사 |
| 전 과정 운영 자동화 | 조건부 가능 | repository별 인수·배포·정상 판정·복구 조건 필요 |

## 4. Actions를 제거하면 이동하는 책임

| 기존 담당 | App 직접 연동 안의 담당 |
| --- | --- |
| 이벤트 workflow | 프로그램의 webhook 수신·검증 모듈 |
| job·workflow 연결 | 명시적 작업 단계 엔진 |
| Actions 실행 대기·동시성 | 프로그램 내부 대기열·잠금·자원 제한 |
| Runner와 작업 공간 | 사내 호스트의 격리된 자식 프로세스·컨테이너 |
| timeout·실행 취소 | 프로세스 감독·종료·안전한 중단 지점 |
| artifact·실행 로그 | 내부 파일 보존·정리·복구 정책 |
| 실행 재시도·조회 | 기록 검증과 실제 GitHub·배포 상태 재조정 |
| CI 검사 결과 | 실제 검증에 근거한 App의 검사 결과 게시 |
| 배포 실행·승인 | 독립 배포 어댑터와 명시적인 승인 정책 |

운영할 제품은 하나로 줄일 수 있지만, 이러한 기능의 구현 책임이 없어지는 것은 아니다. 완결성 평가는 정상 경로와 함께 재시작·중복 이벤트·불명확한 배포 결과를 포함해야 한다.

## 5. 기존 workflow와 보호 규칙의 호환성

workflow 파일을 읽는 기능과 workflow를 실행하는 기능을 분리한다. Actions YAML에는 실행 명령 외에도 재사용 Action, 조건·표현식, job 관계, matrix, secret 참조, 서비스 환경 등이 들어갈 수 있다. 이를 일반 shell 스크립트로 취급하거나 의미가 동일하다고 가정해 실행하지 않는다.

현재 빌드·검증·배포가 repository의 명시적인 스크립트나 내부 도구로 정의되어 있으면 같은 명령을 Agent의 실행 어댑터에서 호출할 수 있다. Actions에만 정의된 절차는 검토된 독립 명령·어댑터로 옮겨야 한다. Agent가 모든 Actions 동작을 호환 구현하는 방향은 추천하지 않는다.

완전 제거 전에 확인할 사항:

1. 필수 검사: 기존 workflow가 생성하던 검사를 무엇이 수행하고 게시할지 정의한다. 특정 검사 이름·발행 주체·commit을 요구하는 보호 규칙은 실제 설정을 확인한다. 이름만 같은 성공 상태를 만들어 기존 검증을 대체하지 않는다.
2. 배포 승인: 기존 Actions 실행을 전제로 한 환경 승인·secret 제공·동시성 조건이 직접 배포 호출에 자동 적용된다고 가정하지 않는다. 요구된 정책을 직접 실행 경로에도 보장하거나 기존 경로를 유지해야 한다.
3. 실행 환경: Runner에 있던 도구·내부 이미지·서비스·자격 증명의 공급과 격리를 대체한다.
4. 실행 근거: 테스트를 수행한 commit과 배포 산출물을 연결하고, 건너뛴 검사를 성공으로 표시하지 않는다.
5. 규칙 변경: 기존 보호 규칙이나 workflow 변경이 필요하면 별도 검토 대상이다. Agent가 편의를 위해 자동 완화하지 않는다.

Agent 프로그램은 Actions 없이 구현하되, 기존 repository의 CI를 유지하는 혼합 운영도 기술적으로 가능하다. 그러나 Actions의 완전 제거를 목표로 한다면 그 CI·배포 책임의 이관까지 완료 조건에 포함해야 한다.

## 6. DB 없는 운영의 구체적인 경계

GitHub에는 요구사항·정리본·합의·승인 근거·PR·commit·최종 검사 결과를 남긴다. Agent 호스트에는 아직 처리되지 않은 이벤트, 작업별 checkpoint, 로컬 실행 로그와 산출물을 파일로 보관한다. SQLite 등 다른 DB를 우회 도입하지 않는다.

운영 조건:

- 상태 디렉터리는 프로세스나 컨테이너 재시작 후에도 남는 보호된 영속 디스크여야 한다.
- webhook은 출처를 확인하고 처리할 이벤트를 지속 저장한 후 수신 완료를 응답한다. 긴 모델·빌드 작업은 수신 요청과 분리한다.
- 프로그램 중단 중 전달되지 않은 webhook이 자동으로 다시 온다고 가정하지 않는다. GHES가 지원하는 전달 이력·재전송 기능과 범위가 제한된 API 재조회를 검토하고 누락을 확인한다. 삭제된 원문처럼 재구성할 수 없는 자료는 복구됐다고 표시하지 않는다.
- task·event 식별자, schema version, 요구사항·설계 revision, commit·정책 revision을 기록한다.
- 파일 갱신 중 종료와 중복 수신을 다룬다. 임시 파일·원자적 교체·검증·파일 잠금 등 구현과 복구를 시험한다.
- 단일 인스턴스가 상태 디렉터리를 소유한다. 여러 독립 인스턴스가 같은 파일을 동시에 수정하는 구조는 기본 범위에 넣지 않는다.
- 백업·복원·보존·디스크 부족·기록 손상 처리가 있어야 한다. 파일을 쓴다는 사실만으로 복구가 보장되지는 않는다.
- 재시작 후 push·PR·배포의 실제 수행 여부를 조회한다. 효과가 불명확하면 그대로 재실행하지 않고 확인 상태로 둔다.
- 로그·checkpoint에는 비밀정보를 포함하지 않는다. 코드 실행 sandbox가 제어 상태·인증 파일을 수정하지 못하게 한다.

단일 호스트 장애 동안 실행 중단이 발생할 수 있다. 높은 가용성이나 여러 호스트의 active-active 실행이 필수이면 상태 공유·조정 방식이 추가로 필요하며, 현재 파일 기반 단일 인스턴스 안으로 충족한다고 주장하지 않는다. 허용 중단 시간과 복구 목표를 운영 기준으로 확인해야 한다.

## 7. 보안과 실행 격리

모든 네트워크 목적지는 사내 승인 범위로 고정한다. webhook 수신도 사내 GHES와 Agent 사이에서 이루어진다. 외부 tunnel, public webhook relay, 원격 모델 fallback은 사용하지 않는다.

코드·빌드 스크립트를 App 개인 키나 운영 권한을 가진 제어 프로세스 안에서 그대로 실행하지 않는다. 같은 프로그램이 별도 권한의 자식 프로세스·컨테이너를 실행하고, 필요한 범위의 입력만 전달한다.

App private key, 모델 인증, Git 쓰기 권한, 배포 권한은 용도별로 분리한다. 빌드·테스트 환경에는 기본적으로 배포 권한을 제공하지 않는다. 사용자가 올린 코드가 제어 프로그램 파일, 상태 디렉터리, secret, 다른 작업 공간에 접근하지 못하게 한다.

패키지·이미지·모델·도구 공급과 업데이트 절차도 내부 공급원으로 고정한다. 외부 목적지 요청은 전송 전에 거부하고 네트워크 격리를 추가 방어선으로 둔다. 검증을 위해 실제 외부 접속을 시도하지 않는다.

## 8. 기술 선택에 미치는 영향

- Python은 계속 유효한 구현 후보다.
- HTTP 수신 라이브러리는 같은 프로그램 안에 포함한다. FastAPI·ASGI 등의 구체적인 선택은 내부 가용성과 운영 방식으로 정한다.
- Pydantic, HTTPX, GitHub App 서명·인증 라이브러리, 안전한 YAML 처리, 파일·프로세스 제어가 주요 구성이다.
- 별도 DB·ORM·외부 queue는 요구하지 않는다.
- Agent SDK는 모델·격리·중단·외부 접속 금지 검증 후 결정한다. 이 평가만으로 OpenHands 등의 실제 호환성을 보장하지 않는다.
- OS service 또는 내부 컨테이너 운영 방식으로 단일 프로그램을 시작·중단·재시작한다. 사용자용 GUI는 필요하지 않다.

## 9. 채택 판단에 필요한 내부 확인

1. GHES에서 Agent 호스트의 내부 webhook endpoint에 접근 가능한가?
2. 프로그램을 계속 실행할 사내 호스트와 보호된 영속 디스크가 있는가?
3. 코드·빌드·테스트를 권한 분리된 프로세스·컨테이너로 실행할 수 있는가?
4. 기존 Actions의 빌드·검증·배포를 대체할 독립 명령이 있는가? 없으면 이관 범위는 무엇인가?
5. 필수 검사·배포 승인·인증 등 기존 Actions 의존 규칙은 무엇인가?
6. 단일 인스턴스의 중단·재시작 특성이 요구 가용성·복구 목표를 만족하는가?
7. 사내 모델 API와 GHES에서 필요한 이벤트·API·권한을 제공하는가?

권고: Agent 실행을 Actions와 분리하는 방향은 채택 가치가 높다. App 직접 연동 단일 애플리케이션을 기본 후보로 삼되, 기존 CI·배포까지의 완전 제거 여부는 4·5번을 확인한 뒤 결정한다. 구현 전 확인은 전부 허용된 내부 자료와 환경에서 진행한다.
